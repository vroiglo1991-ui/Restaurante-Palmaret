/* ══════════════════════════════════════
   EQUIPO
══════════════════════════════════════ */

.eq-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  margin-top: 4rem;
}

.cc {
  border: 1px solid rgba(201,168,76,.15);
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 2rem;
  padding: 2.5rem;
  position: relative;
  overflow: hidden;
  transition: border-color .3s;
}
.cc:hover { border-color: rgba(201,168,76,.4); }
.cc::before {
  content: '';
  position: absolute;
  top: 0; left: 0;
  width: 3px; height: 0;
  background: var(--gold);
  transition: height .4s;
}
.cc:hover::before { height: 100%; }

/* Foto */
.cph {
  width: 80px; height: 80px;
  border-radius: 50%;
  overflow: hidden;
  border: 2px solid rgba(201,168,76,.25);
  flex-shrink: 0;
}
.cph-inner {
  width: 100%; height: 100%;
  background: linear-gradient(145deg, #2a4a6e, #0f2540);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--fd);
  font-size: 1.8rem;
  font-weight: 300;
  color: rgba(201,168,76,.4);
}

/* Textos */
.crl {
  font-family: var(--fc);
  font-size: .62rem;
  font-weight: 700;
  letter-spacing: .2em;
  text-transform: uppercase;
  color: var(--gold);
  margin-bottom: .3rem;
}
.cnm {
  font-family: var(--fd);
  font-size: 1.55rem;
  font-weight: 400;
  color: var(--cream);
  margin-bottom: .7rem;
}
.cbi {
  font-size: .82rem;
  font-weight: 300;
  line-height: 1.7;
  color: rgba(237,232,213,.52);
  margin-bottom: 1rem;
}

/* Tags */
.ctg { display: flex; flex-wrap: wrap; gap: .4rem; }
.ctag {
  font-family: var(--fc);
  font-size: .58rem;
  font-weight: 600;
  letter-spacing: .08em;
  text-transform: uppercase;
  background: rgba(201,168,76,.1);
  color: var(--gold);
  padding: .2rem .6rem;
}
